class CredentialsExample {
    constructor(email, password) {
        this.email = email;
        this.password = password
    }
}

export const Credentials = new CredentialsExample('userexample','passwordexample')